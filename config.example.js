// config.example.js
//
// Template for config.js. Copy this file to config.js and replace the
// placeholder below with your own free Alpha Vantage key:
// https://www.alphavantage.co/support/#api-key
//
// config.js is gitignored — config.example.js is what actually gets
// committed, so nobody's real key ends up in the repo history.

window.CONFIG = {
  ALPHA_VANTAGE_API_KEY: "YOUR_API_KEY_HERE",
};
